# Exemplo de variáveis de ambiente para JobFishing Backend

# Porta para rodar o backend localmente (se aplicável)
PORT=8000

# Domínio base do encurtador
SHORTENER_DOMAIN=https://jfsh.io

# Domínio principal do site
MAIN_DOMAIN=https://jobfising.us

# Banco de dados (se for adicionar depois)
DATABASE_URL=postgresql://usuario\:senha\@localhost:5432/jobfishing

# Chave secreta para autenticação (JWT, etc. no futuro)
SECRET_KEY=troque-essa-chave

# Ambiente de execução
ENV=production
